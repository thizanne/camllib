
First copy Makefile.config.model into Makefile.config and set properly the
flags (mainly directories).

To compile the library:
=======================
make all # for bytecode and nativecode compilation

To install the library:
=======================
set properly the PREFIX and LIBDIR variable in the Makefile
make install

